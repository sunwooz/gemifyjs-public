require "<%= @jem.name %>/version"

module <%= module_name %>
  module Rails
    if ::Rails.version < "3.1"
      require "<%= @jem.name %>/railtie"
    else
      require "<%= @jem.name %>/engine"
    end
  end
end
